import { ResumeOnboarding } from "@/components/ai/onboarding/ResumeOnboarding";

/** /ai/start: straight to the first step with something left to do. */
export default function StartPage() {
  return <ResumeOnboarding />;
}
