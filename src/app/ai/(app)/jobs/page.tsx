import { PagePlaceholder } from "@/components/ai/shell/PagePlaceholder";

export const metadata = { title: "Jobs · Nasuru AI" };

export default function JobsPage() {
  return (
    <PagePlaceholder
      title="Jobs"
      what="Verified jobs from employers who can sponsor you."
      module="F8"
    />
  );
}
