import { SavedJobsView } from "@/components/ai/jobs/SavedAndAlerts";

export const metadata = { title: "Saved jobs · Nasuru AI" };

export default function SavedJobsPage() {
  return <SavedJobsView />;
}
