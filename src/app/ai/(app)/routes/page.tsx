import { PagePlaceholder } from "@/components/ai/shell/PagePlaceholder";

export const metadata = { title: "Your routes · Nasuru AI" };

export default function RoutesPage() {
  return (
    <PagePlaceholder
      title="Your routes"
      what="Every route in your destination, with what you meet and what's missing."
      module="F7"
    />
  );
}
